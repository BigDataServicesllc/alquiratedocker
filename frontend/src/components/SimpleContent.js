import React from 'react';

const SimpleContent = () => {
  return (
    <div className="container mx-auto px-4 py-8 text-center">
      <h2 className="text-2xl font-semibold text-gray-800 mb-4">¡Hola desde la Base Estable!</h2>
      <p className="text-gray-600">Esta es una aplicación simple para verificar que todo carga correctamente.</p>
    </div>
  );
};

export default SimpleContent;